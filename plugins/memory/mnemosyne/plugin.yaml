name: mnemosyne
description: Local-only trusted explicit recall memory provider with auditable candidate queue, add/recall/hygiene/suppress/unsuppress controls, supersession metadata, and gated observable selective prefetch.
version: 0.4.0
